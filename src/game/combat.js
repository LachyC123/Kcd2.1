(() => {
  'use strict';
  const LW = window.LW;

  // Combat system stub (melee/block/dodge/telegraphs) will be implemented after baseline world+NPCs exist.
  LW.combat = {
    update() {},
  };
})();

